type Query {
  post(id: Int!): Post!
  posts: [Post!]!
}

type Mutation {
  createPost(data: CreatePostInput): Post!
}

type Post {
  id: ID!
  title: String!
  body: String!
  published: Boolean!
  author: User!
}

input CreatePostInput {
  title: String!
  body: String!
  author: Int!
}
